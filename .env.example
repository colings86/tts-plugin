# TTS Plugin Configuration
# Copy this file to ~/.claude/tts-plugin.env and customize as needed

# ============================================================================
# Master Enable/Disable Switches
# ============================================================================

# Global TTS enable/disable (affects all hooks)
TTS_ENABLED=true

# PreToolUse hook enable/disable (only active if TTS_ENABLED=true)
# Note: PreToolUse hook can be verbose as it speaks before each tool execution
TTS_PRETOOL_ENABLED=false

# ============================================================================
# Voice Configuration
# ============================================================================

# Voice to use (run 'kokoro-tts --help-voices' to see all available voices)
# Common voices: af_bella, af_sarah, af_sky, bf_emma, bf_isabella
TTS_VOICE=af_bella

# Language (run 'kokoro-tts --help-languages' to see all available languages)
# Common languages: en-gb, en-us, es, fr, de, ja
TTS_LANG=en-gb

# Speech speed (0.5-2.0, where 1.0 is normal speed)
TTS_SPEED=1.3

# ============================================================================
# Model Paths
# ============================================================================

# Path to kokoro-tts model file
TTS_MODEL=$HOME/.local/share/kokoro-tts/kokoro-v1.0.onnx

# Path to kokoro-tts voices file
TTS_VOICES=$HOME/.local/share/kokoro-tts/voices-v1.0.bin

# ============================================================================
# Text Processing Configuration
# ============================================================================

# Extract "## TTS Response" section from Claude's response if present
# When true, only speaks the TTS-optimized section
# When false, speaks the entire response
TTS_USE_TTS_SECTION=true

# Maximum characters to speak per message (prevents extremely long TTS)
TTS_MAX_LENGTH=5000

# ============================================================================
# State and Logging
# ============================================================================

# Directory for session state files (tracks what's been spoken)
TTS_STATE_DIR=$HOME/.local/state/claude-tts/session-state

# Directory for TTS log files
TTS_LOG_DIR=$HOME/.local/state/claude-tts/logs
